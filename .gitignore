# Build results
bin/
obj/
*.user
*.suo
.vs/
*.nupkg

# IDE
.idea/
.vscode/

# Local data
*.log

# 运行时（NapCat 二进制/日志/会话数据）不纳入版本库。
# ⚠ 必须锚定到仓库根：不锚定的话 `napcat/` 会连 `src/.../Services/NapCat/` 一起吞掉 ——
# LoginQrService.cs 就这么从仓库里"消失"了很久（本地能编译、公开快照里缺文件、clone 下来编译不过）。
/runtime/

# 容器部署产物（同样锚定：目录名太通用，不锚定会误伤源码目录）
/data/
/napcat/
.env
.env.*
!.env.example

# 参考源码（vendor/NapCatQQ 等）不纳入仓库
/vendor/

# Python（工具脚本的字节码缓存，不进版本库）
__pycache__/
*.pyc
